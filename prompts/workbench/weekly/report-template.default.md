请基于本周项目与任务进展，输出一份仅包含“项目 + 任务清单”的结构化内容。

严格按如下 Markdown 列表格式输出：

* 项目名称
    * 任务描述（状态）
    * 任务描述（状态）

要求：

- 只输出本周项目及任务清单，不要生成总结、结论、风险总览、下周计划、开场白或结束语
- 顶层只能是项目列表，第二层只能是该项目下的任务列表
- 每条任务都要在末尾明确标注当前完成情况，状态统一收敛为“已完成 / 进行中 / 待开始 / 受阻”
- 允许对任务描述做轻量专业化优化，让表达更清晰、更像正式项目推进项，但不能改动事实，不能补充原文没有的信息
- 优先保留项目归属；如果原始内容已经按项目组织，则不要打散
- 任务描述尽量写成“动作 + 结果/对象”的短句，不要写成长段说明
- 如果某个项目下没有有效任务，则不要为了凑格式编造任务
- 输出内容必须简洁清晰，适合直接作为本周任务清单展示
